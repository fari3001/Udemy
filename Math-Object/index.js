const num1 = 100;
const num2 = 50; 
let val;

// ------------------------ MATH OBJECT ---------------------------
//                  has properties and methods 
// Properties: attribute 
// Method: function 

 val = Math.PI; // get pi
 val = Math.E;
 val = Math.round(2.4); // rounds the number to the nearest whole number
 val = Math.ceil(2.4); // rounds up
 val = Math.floor(2.8); // rounds down
 val = Math.sqrt(64); // gets square root of number
 val = Math.abs(-3); // gets the absolute number 
 val = Math.pow(8, 2); // number to the power of another number
 val = Math.min(2, 33, 24, 5, 1); // finds the minumum value 
 val = Math.max(2, 33, 24, 5, 1); // finds the maximum value
 val = Math.random(); // ouputs random number

 val = Math.floor(Math.random() * 20 + 1) // computes random number up to 19 (+1 to get 20) which is a whole number rounded down

//------------------------------------------------------------------

console.log(val)
